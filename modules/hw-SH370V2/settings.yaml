vars:
  font-0-size: '10'
  font-1-size: '12'

display:
  primary: HDMI-2
  secondary: DP-1
  layout: xrandr --output DP-1 --left-of HDMI-2

polybar:
  networks:
  - br1
  - enp2s0

  options:
  - kube-tools

i3:
  options:
  - kube-tools
  - gaps

  # modes:
  # - name: devbox
  #   type: mode
  #   label: 'devbox'
  #   rebind: true
  #   bindings:
  #     "$mod+Return": 'exec ssh devbox alacritty'
  #     "$mod+p": 'exec ssh devbox rofi -theme glue_pro_blue -show drun'

  bindings:
    #"$mod+b": 'mode "devbox"'
    #"$mod+Shift+b": 'mode "default"'
    "$mod+Shift+Return": 'exec gnome-terminal -- ssh devbox'
    "$mod+Shift+p": 'exec ssh devbox rofi -theme glue_pro_blue -show drun'
    "$mod+p": 'exec --no-startup-id "rofi -font \\"DejaVu Sans Mono 6\\" -combi-modi window,drun,calc -dpi 192 -theme rofi -show combi -display-window \\"\\" -display-drun \\"\\" -display-calc \\"\\" -display-combi \\">> \\""'
