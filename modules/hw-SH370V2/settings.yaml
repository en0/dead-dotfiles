vars:
  ssh_target: vm-work

display:
    primary: HDMI-2
    secondary: DP-1
    layout: xrandr --output DP-1 --left-of HDMI-2

polybar:
    networks:
     - enp1s0
     - enp2s0

common:
  with_kube_tools: True

i3:
  options:
    - kube_tools
    - gaps
    - ssh-targets
